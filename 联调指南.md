# 在线点餐系统 - 前后端联调指南

## 项目结构
```
Online-Catering-System-main/
├── backend/          # 后端 (Spring Boot)
├── frontend/         # 前端
│   ├── admin/       # 管理端 (Vue3 + Vite)
│   └── user/        # 用户端 (微信小程序)
```

## 环境要求

### 后端环境
- **JDK**: 1.8 或以上
- **Maven**: 3.6+
- **MySQL**: 5.7 或 8.0
- **Redis**: 可选（配置文件中已配置但pom.xml未添加依赖，如需使用请添加依赖）

### 前端环境
- **Node.js**: 14+ 
- **npm** 或 **pnpm**

---

## 一、数据库配置

### 1. 创建数据库
```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE reggie DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

# 退出
exit;
```

### 2. 导入数据
```bash
# 在项目根目录执行
cd backend
mysql -u root -p reggie < db.sql
```

### 3. 修改数据库配置
编辑 `backend/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/reggie?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: root          # 修改为你的MySQL用户名
      password: 123456        # 修改为你的MySQL密码
```

---

## 二、后端启动

### 方式一：使用IDE（推荐）
1. 用 IDEA 打开 `backend` 目录
2. 等待 Maven 依赖下载完成
3. 找到主类 `ReggieApplication.java`（通常在 `src/main/java/com/java_project/reggie/` 下）
4. 右键运行 `Run 'ReggieApplication'`

### 方式二：使用Maven命令
```bash
cd backend
mvn clean install
mvn spring-boot:run
```

### 验证后端启动成功
- 控制台显示：`Started ReggieApplication in xxx seconds`
- 访问：http://localhost:8080
- 后端默认端口：**8080**

---

## 三、前端启动

### 管理端（Admin）

#### 1. 安装依赖
```bash
cd frontend/admin
npm install
# 或使用 pnpm
pnpm install
```

#### 2. 启动开发服务器
```bash
npm run dev
# 或
pnpm dev
```

#### 3. 访问管理端
- 浏览器打开：http://localhost:3000
- 前端默认端口：**3000**

#### 4. 代理配置说明
前端已配置代理，所有 `/api` 请求会自动转发到后端 `http://localhost:8080`

查看配置：`frontend/admin/vite.config.js`
```javascript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, '')
    }
  }
}
```

---

## 四、联调测试

### 1. 测试流程
1. ✅ 确保后端已启动（端口 8080）
2. ✅ 确保前端已启动（端口 3000）
3. ✅ 打开浏览器访问：http://localhost:3000
4. ✅ 测试登录功能
5. ✅ 测试各个功能模块

### 2. 常见问题排查

#### 问题1：前端无法访问后端API
- **检查后端是否正常启动**（访问 http://localhost:8080）
- **检查端口是否被占用**
- **查看浏览器控制台Network选项**，确认请求是否发送成功

#### 问题2：数据库连接失败
```
错误：Access denied for user 'root'@'localhost'
解决：检查 application.yml 中的数据库用户名和密码
```

#### 问题3：跨域问题
- 后端需要配置CORS，查看是否有 `@CrossOrigin` 注解或全局CORS配置

#### 问题4：Redis连接失败
```
如果不需要Redis，可以：
1. 注释掉 application.yml 中的 Redis 配置
2. 或在本地启动 Redis 服务
```

---

## 五、配置说明

### 后端配置文件
**文件位置**：`backend/src/main/resources/application.yml`

```yaml
server:
  port: 8080                    # 后端端口

spring:
  datasource:                   # 数据库配置
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/reggie...
      username: root
      password: 123456
  
  redis:                        # Redis配置（可选）
    host: localhost
    port: 6379
    password: 
    database: 0

reggie:
  path: F:/开发项目/接单/CM/Online-Catering-System-main/backend/src/main/resources/food_img/
  # 图片上传路径，根据实际情况修改
```

### 前端配置文件
**文件位置**：`frontend/admin/src/api/request.js`

```javascript
const service = axios.create({
  baseURL: '/api',      // 通过Vite代理转发
  timeout: 10000
})
```

---

## 六、快速启动脚本

### Windows PowerShell

**启动后端**：
```powershell
cd backend
mvn spring-boot:run
```

**启动前端（新开一个终端）**：
```powershell
cd frontend/admin
npm run dev
```

---

## 七、端口汇总

| 服务 | 端口 | 访问地址 |
|------|------|----------|
| 后端API | 8080 | http://localhost:8080 |
| 前端管理端 | 3000 | http://localhost:3000 |
| MySQL | 3306 | localhost:3306 |
| Redis（可选） | 6379 | localhost:6379 |

---

## 八、API测试

使用浏览器或Postman测试后端API：

```bash
# 测试后端健康状态
GET http://localhost:8080/

# 测试具体API（需根据实际Controller定义）
GET http://localhost:8080/employee/list
POST http://localhost:8080/employee/login
```

---

## 九、注意事项

1. **确保端口不冲突**：8080（后端）和 3000（前端）端口未被占用
2. **数据库编码**：使用 UTF-8 编码，避免中文乱码
3. **图片路径**：确保 `reggie.path` 配置的路径存在且有读写权限
4. **防火墙**：确保防火墙允许相关端口通信
5. **Redis**：如果不使用Redis，建议注释相关配置或添加Redis依赖并启动Redis服务

---

## 十、开发建议

1. **前端调试**：使用浏览器开发者工具查看Network请求
2. **后端调试**：查看IDEA控制台日志
3. **数据库调试**：使用Navicat或DataGrip查看数据
4. **接口文档**：建议使用Swagger或Knife4j生成API文档

---

## 问题反馈

如遇到问题，请检查：
1. 后端控制台日志
2. 前端浏览器控制台
3. MySQL数据库连接状态
4. 端口占用情况

**祝你联调顺利！** 🚀

