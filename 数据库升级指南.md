# 数据库升级指南

## 概述

本次升级添加了以下重要功能：
1. **餐厅管理系统** - 支持多个餐厅，菜品关联到具体餐厅
2. **用户余额系统** - 用户可以充值、消费、查看余额
3. **优惠券系统** - 用户可以领取和使用优惠券
4. **积分系统** - 消费获得积分，积分可兑换

## 数据库变更内容

### 新增表
- `canteen` - 餐厅信息表
- `user_coupon` - 用户优惠券表
- `points_record` - 积分记录表
- `balance_record` - 余额变动记录表

### 修改表
- `dish` - 添加 `canteen_id` 字段，关联餐厅
- `user` - 添加 `balance`、`coupon_count`、`points` 字段

### 测试数据
- 3个餐厅（第一食堂、第二食堂、第三食堂）
- 4个测试用户（带余额、优惠券、积分）
- 菜品已分配到各个餐厅
- 用户地址数据

## 升级步骤

### 1. 备份现有数据库（重要！）

```bash
mysqldump -u root -p reggie > reggie_backup_$(date +%Y%m%d).sql
```

### 2. 执行升级SQL

```bash
# 方式1: 使用命令行
mysql -u root -p reggie < backend/db_update.sql

# 方式2: 使用Navicat等工具
# 打开 backend/db_update.sql 文件，直接执行
```

### 3. 验证升级结果

登录MySQL，检查表结构和数据：

```sql
USE reggie;

-- 检查新表是否创建成功
SHOW TABLES LIKE 'canteen';
SHOW TABLES LIKE 'user_coupon';
SHOW TABLES LIKE 'points_record';
SHOW TABLES LIKE 'balance_record';

-- 检查字段是否添加成功
DESC dish;  -- 查看是否有 canteen_id 字段
DESC user;  -- 查看是否有 balance、coupon_count、points 字段

-- 检查测试数据
SELECT * FROM canteen;
SELECT id, name, phone, balance, coupon_count, points FROM user;
SELECT id, name, canteen_id FROM dish LIMIT 10;
```

## 测试账号

执行完SQL后，可以使用以下测试账号登录小程序：

| 手机号 | 姓名 | 余额 | 优惠券 | 积分 |
|--------|------|------|--------|------|
| 13800138000 | 张三 | 98.50 | 3 | 1280 |
| 13800138001 | 李四 | 150.00 | 5 | 800 |
| 13800138002 | 王五 | 200.00 | 2 | 500 |
| 13800138003 | 赵六 | 50.00 | 1 | 200 |

**登录方式**：小程序端输入手机号即可登录（已配置测试环境）

## 功能测试清单

### 1. 餐厅功能
- [ ] 首页显示餐厅列表
- [ ] 点击餐厅进入菜单页
- [ ] 菜单页显示该餐厅的菜品

### 2. 用户功能
- [ ] 用户页显示余额、优惠券、积分
- [ ] 余额、优惠券、积分数据从后端获取

### 3. 点餐功能
- [ ] 添加菜品到购物车
- [ ] 提交订单
- [ ] 查看订单列表

### 4. AI推荐功能
- [ ] AI聊天界面显示正常
- [ ] AI推荐菜品
- [ ] 点击推荐菜品跳转到菜单页

## 常见问题

### Q1: 执行SQL时报错 "Table 'canteen' already exists"
**A**: 这表明表已经存在，可以：
1. 跳过该错误继续执行
2. 或者手动删除旧表后重新执行

### Q2: 菜品没有关联到餐厅
**A**: 检查执行SQL时是否成功更新了dish表的canteen_id字段：
```sql
SELECT canteen_id, COUNT(*) FROM dish GROUP BY canteen_id;
```

### Q3: 用户余额等字段显示NULL
**A**: 检查user表是否成功添加了新字段：
```sql
DESC user;
```

如果字段存在但值为NULL，更新默认值：
```sql
UPDATE user SET balance = 0 WHERE balance IS NULL;
UPDATE user SET coupon_count = 0 WHERE coupon_count IS NULL;
UPDATE user SET points = 0 WHERE points IS NULL;
```

## 回滚方案

如果升级后出现问题，可以回滚到备份：

```bash
# 先删除当前数据库
mysql -u root -p -e "DROP DATABASE reggie;"

# 创建新数据库
mysql -u root -p -e "CREATE DATABASE reggie CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;"

# 恢复备份
mysql -u root -p reggie < reggie_backup_YYYYMMDD.sql
```

## 后续开发建议

1. **优惠券系统完善**
   - 实现优惠券领取功能
   - 实现下单时使用优惠券
   - 实现优惠券过期处理

2. **积分系统完善**
   - 实现消费自动获得积分
   - 实现积分兑换功能
   - 实现积分排行榜

3. **余额系统完善**
   - 实现充值功能
   - 实现余额支付
   - 实现余额变动记录查询

4. **餐厅功能扩展**
   - 实现餐厅营业时间判断
   - 实现餐厅评分和评论
   - 实现餐厅搜索和筛选

## 联系支持

如有问题，请联系开发团队或查看项目文档。

