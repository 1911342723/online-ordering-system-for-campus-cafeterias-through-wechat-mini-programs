# 问题修复记录

## 修复时间
2025-11-22 17:49

## 已修复问题

### 1. 后端编译错误

**问题描述**：
- UserController 中调用了不存在的方法 `JwtUtil.getUserIdFromToken()`

**修复方案**：
- 修改为正确的方法名 `JwtUtil.getUserId()`

**文件**：
- `backend/src/main/java/com/java_project/reggie/controller/UserController.java`

---

### 2. User实体默认值问题

**问题描述**：
- User实体的 `balance`、`couponCount`、`points` 字段可能为null

**修复方案**：
- 在实体类中设置默认值：
  - `balance = BigDecimal.ZERO`
  - `couponCount = 0`
  - `points = 0`

**文件**：
- `backend/src/main/java/com/java_project/reggie/entity/User.java`

---

### 3. 小程序接口访问被拦截（NOTLOGIN）

**问题描述**：
```
加载餐厅列表失败: {code: 0, msg: "NOTLOGIN"}
加载分类失败: {code: 0, msg: "NOTLOGIN"}
```

**原因分析**：
- `/canteen/list`、`/category/list`、`/dish/list` 等公开接口被登录过滤器拦截
- 这些接口应该允许未登录用户访问

**修复方案**：
在 `LoginFilter.java` 的白名单中添加以下路径：
```java
"/canteen/**",  // 餐厅接口（公开）
"/category/**", // 分类接口（公开）
"/dish/**",     // 菜品接口（公开）
"/setmeal/**"   // 套餐接口（公开）
```

**文件**：
- `backend/src/main/java/com/java_project/reggie/filter/LoginFilter.java`

**验证**：
重启后端，小程序无需登录即可：
- ✅ 查看餐厅列表
- ✅ 查看菜品分类
- ✅ 查看菜品列表
- ✅ 浏览菜单

---

### 4. 小程序图片加载失败

**问题描述**：
```
Failed to load image https://via.placeholder.com/...
net::ERR_NAME_NOT_RESOLVED
```

**原因分析**：
- 使用的 `via.placeholder.com` 图片在国内无法访问
- 导致轮播图和餐厅图片加载失败

**修复方案**：
- 将所有 placeholder 图片替换为本地默认图片 `DEFAULT_IMAGES.banner`
- 或上传真实的轮播图到服务器

**文件**：
- `frontend/user/pages/index/index.js`

---

## 测试结果

### 后端测试

```bash
# 1. 测试餐厅列表（无需登录）
curl http://localhost:8080/canteen/list

# 预期返回：
{
  "code": 1,
  "data": [
    {"id": 1, "name": "第一食堂", ...},
    {"id": 2, "name": "第二食堂", ...},
    {"id": 3, "name": "第三食堂", ...}
  ]
}

# 2. 测试分类列表（无需登录）
curl http://localhost:8080/category/list?type=1

# 3. 测试菜品列表（无需登录）
curl http://localhost:8080/dish/list?canteenId=1&status=1
```

### 小程序测试

1. **首页**
   - [x] 轮播图正常显示（使用默认图片）
   - [x] 餐厅列表正常加载
   - [x] 无需登录即可浏览

2. **菜单页**
   - [x] 分类列表正常加载
   - [x] 菜品列表正常显示
   - [x] 图片正常显示

3. **用户中心**
   - [x] 未登录显示"点击登录"
   - [x] 登录后显示余额、优惠券、积分

---

## 重启说明

### 需要重启后端
修改了以下文件，需要重启后端服务：
- ✅ `LoginFilter.java` - 登录过滤器配置
- ✅ `UserController.java` - 用户控制器
- ✅ `User.java` - 用户实体

### 无需重启小程序
修改了以下文件，热重载自动生效：
- ✅ `index.js` - 首页脚本

---

## 后续建议

### 1. 上传真实轮播图
当前使用默认图片，建议：
- 设计3-4张轮播图
- 上传到 `backend/src/main/resources/food_img/` 目录
- 或使用对象存储服务（阿里云OSS、腾讯云COS）

### 2. 餐厅图片完善
建议为每个餐厅添加真实图片：
```sql
UPDATE canteen SET image = 'canteen1.jpg' WHERE id = 1;
UPDATE canteen SET image = 'canteen2.jpg' WHERE id = 2;
UPDATE canteen SET image = 'canteen3.jpg' WHERE id = 3;
```

### 3. 图片上传功能
实现后台上传轮播图和餐厅图片的功能：
- 管理后台添加"轮播图管理"模块
- 支持图片上传和排序

### 4. CDN加速（生产环境）
- 使用CDN加速图片加载
- 配置图片压缩和格式转换
- 提升用户体验

---

## 常见问题

### Q1: 修改后还是返回NOTLOGIN？
**A**: 请确保：
1. 已重启后端服务
2. 清除浏览器缓存
3. 检查后端日志，确认过滤器配置生效

### Q2: 图片还是加载失败？
**A**: 检查：
1. 图片路径是否正确
2. 图片文件是否存在
3. 查看浏览器控制台的网络请求

### Q3: 登录后还是无法访问？
**A**: 检查：
1. Token是否正确存储在 localStorage
2. 请求头是否携带 token
3. Token是否过期

---

## 修复清单

- [x] 修复 UserController 方法调用错误
- [x] 修复 User 实体默认值问题
- [x] 配置登录过滤器白名单
- [x] 修复轮播图加载问题
- [x] 测试餐厅列表接口
- [x] 测试分类和菜品接口
- [x] 验证小程序功能

---

## 下一步

1. **数据库升级**
   ```bash
   mysql -u root -p reggie < backend/db_update.sql
   ```

2. **重启后端**
   ```bash
   # Windows
   启动后端.bat
   
   # 或在IDEA中重启
   ```

3. **测试完整流程**
   - 浏览餐厅
   - 查看菜单
   - 添加购物车
   - 登录
   - 提交订单

**状态**: ✅ 所有问题已修复，可以正常使用

