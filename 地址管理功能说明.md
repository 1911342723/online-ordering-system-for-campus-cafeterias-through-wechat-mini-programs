# 地址管理功能说明

## 问题描述

用户在下单时遇到"用户地址信息有误，不能下单"的错误，原因是：

1. **前端缺少地址管理功能**：没有地址列表、添加、编辑、删除地址的页面
2. **后端逻辑问题**：即使选择"到店自取"，也要求提供地址信息

## 解决方案

### 一、新增前端地址管理功能

#### 1. 地址列表页面 (`pages/address/list`)
- **功能**：
  - 显示用户所有收货地址
  - 设置默认地址
  - 编辑地址
  - 删除地址
  - 从结算页面选择地址

- **文件**：
  - `frontend/user/pages/address/list.wxml`
  - `frontend/user/pages/address/list.wxss`
  - `frontend/user/pages/address/list.js`
  - `frontend/user/pages/address/list.json`

#### 2. 地址编辑页面 (`pages/address/edit`)
- **功能**：
  - 新增收货地址
  - 编辑已有地址
  - 表单验证（姓名、电话、地区、详细地址）
  - 设置默认地址

- **文件**：
  - `frontend/user/pages/address/edit.wxml`
  - `frontend/user/pages/address/edit.wxss`
  - `frontend/user/pages/address/edit.js`
  - `frontend/user/pages/address/edit.json`

#### 3. 修改的文件
- `frontend/user/pages/user/user.js`：连接"我的地址"菜单到地址列表页面
- `frontend/user/app.json`：注册新页面
- `frontend/user/pages/order/checkout.js`：修复订单提交成功后跳转

### 二、完善后端API

#### 1. 新增的API

在 `AddressController.java` 中添加：

```java
// 更新地址
@PutMapping
public R<AddressBook> update(@RequestBody AddressBook addressBook)

// 删除地址
@DeleteMapping("/{id}")
public R<String> delete(@PathVariable Long id)
```

#### 2. 修复订单提交逻辑

在 `OrderServiceImpl.java` 中修改：

- **问题**：无论选择自取还是外送，都强制要求提供地址
- **修复**：
  - 只有选择"商家外送"时才需要地址
  - 选择"到店自取"时，使用用户信息作为收货人信息

## 使用流程

### 1. 添加收货地址

```
个人中心 -> 我的地址 -> 添加新地址
```

填写信息：
- 收货人姓名（必填）
- 联系电话（必填，11位手机号）
- 性别（可选）
- 所在地区（必填，选择省市区）
- 详细地址（必填）
- 地址标签（可选：家/公司/学校/自定义）
- 是否设为默认地址

### 2. 选择外送下单

```
购物车 -> 去结算 -> 选择"商家外送" -> 选择收货地址 -> 提交订单
```

### 3. 选择自取下单

```
购物车 -> 去结算 -> 选择"到店自取" -> 提交订单
```

**注意**：自取模式不需要选择地址！

## API说明

### 地址相关API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/addressBook` | 新增地址 |
| PUT | `/addressBook` | 更新地址 |
| DELETE | `/addressBook/{id}` | 删除地址 |
| PUT | `/addressBook/default` | 设置默认地址 |
| GET | `/addressBook/{id}` | 根据ID查地址 |
| GET | `/addressBook/default` | 查默认地址 |
| GET | `/addressBook/list` | 查询用户的全部地址 |

### 订单提交API

**请求**：`POST /order/submit`

```json
{
  "deliveryType": 1,        // 1:自取 2:外送
  "addressBookId": null,    // 外送时必填，自取时为null
  "payMethod": 1,           // 1:微信支付 2:支付宝
  "remark": "少油少盐",     // 备注
  "canteenId": 1,           // 食堂ID
  "canteenName": "第一食堂" // 食堂名称
}
```

## 测试清单

### 前端测试

- [ ] 进入"我的地址"页面，查看地址列表
- [ ] 添加新地址，验证表单验证
- [ ] 编辑已有地址
- [ ] 删除地址
- [ ] 设置默认地址
- [ ] 从结算页面选择地址

### 订单测试

- [ ] 选择"到店自取"，无需地址即可下单
- [ ] 选择"商家外送"，未选地址时提示错误
- [ ] 选择"商家外送"，选择地址后成功下单
- [ ] 验证配送费计算（外送+3元）
- [ ] 验证订单提交后跳转到订单列表

## 注意事项

1. **地址安全**：
   - 所有地址操作都需要验证当前用户身份
   - 不允许修改或删除其他用户的地址

2. **表单验证**：
   - 手机号必须是11位数字
   - 必填字段：收货人、电话、地区、详细地址

3. **默认地址**：
   - 设置新的默认地址时，会自动取消原有的默认地址
   - 每个用户只能有一个默认地址

4. **微信小程序限制**：
   - region picker不提供区划代码，如需要可维护映射表
   - 使用 switchTab 跳转到 tabBar 页面

## 文件清单

### 新增文件
```
frontend/user/pages/address/
├── list.wxml
├── list.wxss
├── list.js
├── list.json
├── edit.wxml
├── edit.wxss
├── edit.js
└── edit.json
```

### 修改文件
```
frontend/user/
├── app.json
└── pages/
    ├── user/user.js
    └── order/checkout.js

backend/src/main/java/com/java_project/reggie/
├── controller/AddressController.java
└── service/impl/OrderServiceImpl.java
```

