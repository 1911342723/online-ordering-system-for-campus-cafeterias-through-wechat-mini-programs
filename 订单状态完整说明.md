# 订单状态完整说明

## 订单状态流程

### 到店自取订单（deliveryType = 1）
```
状态1: 待付款 → 状态2: 制作中 → 状态3: 待取餐 → 状态4: 已完成
```

### 外送订单（deliveryType = 2）
```
状态1: 待付款 → 状态2: 制作中 → 状态3: 派送中 → 状态4: 已完成
```

## 状态码对应

| 状态码 | 自取订单显示 | 外送订单显示 | 说明 |
|--------|-------------|-------------|------|
| 1 | 待付款 | 待付款 | 订单已提交，等待支付 |
| 2 | 制作中 | 制作中 | 已支付，商家准备中 |
| 3 | **待取餐** | **派送中** | 自取：可以去取；外送：正在配送 |
| 4 | 已完成 | 已完成 | 订单完成 |
| 5 | 已取消 | 已取消 | 订单取消 |

## 实现方式

### 前端配置 (`frontend/user/utils/config.js`)

```javascript
// 自取订单状态
const ORDER_STATUS = {
  1: '待付款',
  2: '制作中',
  3: '待取餐',  // 自取特有
  4: '已完成',
  5: '已取消'
}

// 外送订单状态
const ORDER_STATUS_DELIVERY = {
  1: '待付款',
  2: '制作中',
  3: '派送中',  // 外送特有
  4: '已完成',
  5: '已取消'
}

// 根据配送方式获取状态文本
function getOrderStatusText(status, deliveryType) {
  if (deliveryType === 2) {
    return ORDER_STATUS_DELIVERY[status]
  }
  return ORDER_STATUS[status]
}
```

### 订单列表Tab

```
全部 | 待付款 | 制作中 | 进行中 | 已完成
 0       1        2       3        4
```

- **全部**: 显示所有订单
- **待付款**: 筛选状态=1的订单
- **制作中**: 筛选状态=2的订单
- **进行中**: 筛选状态=3的订单（自取显示"待取餐"，外送显示"派送中"）
- **已完成**: 筛选状态=4的订单

### 后端状态定义 (`Orders.java`)

```java
// status字段
// 1: 待支付
// 2: 待派送（商家制作中）
// 3: 已派送（自取=待取餐，外送=派送中）
// 4: 已完成
// 5: 已取消
```

### 状态更新流程

1. **用户下单**: status = 1 (待付款)
2. **支付成功**: status = 2 (制作中/待派送)
3. **商家完成制作**: status = 3
   - 自取订单：显示"待取餐"，提示用户前往取餐
   - 外送订单：显示"派送中"，骑手开始配送
4. **订单完成**: status = 4
   - 自取：用户取走餐品
   - 外送：用户收到餐品

## 订单详情页描述

### 自取订单描述
- 状态1: "订单已提交，请尽快完成支付"
- 状态2: "商家正在准备您的订单"
- 状态3: **"您的订单已准备好，请前往取餐"**
- 状态4: "感谢您的光临，期待再次为您服务"

### 外送订单描述
- 状态1: "订单已提交，请尽快完成支付"
- 状态2: "商家正在准备您的订单"
- 状态3: **"您的订单正在配送中，请注意查收"**
- 状态4: "感谢您的光临，期待再次为您服务"

## 关键点

1. **数据库字段**: `delivery_type` (1=自取, 2=外送)
2. **前端判断**: 根据 `deliveryType` 显示不同的状态文本
3. **后端返回**: 必须包含 `deliveryType` 字段
4. **状态一致性**: 前后端状态码必须保持一致

## 示例

### 自取订单示例
```json
{
  "id": 123,
  "status": 3,
  "deliveryType": 1,
  "statusText": "待取餐"  // 由getOrderStatusText计算得出
}
```

### 外送订单示例
```json
{
  "id": 456,
  "status": 3,
  "deliveryType": 2,
  "statusText": "派送中"  // 由getOrderStatusText计算得出
}
```

