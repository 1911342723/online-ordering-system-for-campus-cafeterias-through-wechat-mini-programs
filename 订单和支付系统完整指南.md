# 订单和支付系统完整指南

## 📋 已完成的功能

### 1. ✅ 订单状态系统修复
- **订单提交后状态**：待支付(1) → 待派送(2) → 已派送(3) → 已完成(4)
- **订单状态显示**：不同状态有不同的颜色标识
- **状态筛选**：可按状态筛选订单列表

### 2. ✅ 支付功能
#### 2.1 钱包余额支付
- 扣除用户余额
- 余额不足提示
- 支付成功后更新订单状态

#### 2.2 第三方支付（Mock）
- 微信支付模拟
- 支付宝支付模拟
- 支付成功后更新订单状态

### 3. ✅ 余额管理
#### 3.1 余额充值
- 快捷金额选择（¥10/50/100/200/500/1000）
- 自定义充值金额
- 微信/支付宝支付方式

#### 3.2 余额显示
- "我的"页面显示当前余额
- 支付页面显示余额
- 充值页面显示余额

### 4. ✅ 地址管理
- 新增/编辑/删除收货地址
- 设置默认地址
- 地址列表展示
- 自取订单不需要地址

### 5. ✅ 订单列表优化
- 显示真实菜品名称和图片
- 显示订单商品数量
- 卡片布局优化，不再超出边界
- 订单状态颜色区分
- 支持状态筛选

---

## 🎯 完整业务流程

### 流程1：下单（到店自取）
```
1. 添加商品到购物车
2. 去结算
3. 选择"到店自取"（无需地址）
4. 填写备注（可选）
5. 提交订单（状态：待支付）
6. 选择支付方式：
   - 钱包余额
   - 微信支付（模拟）
   - 支付宝支付（模拟）
7. 完成支付（状态变为：待派送）
```

### 流程2：下单（商家外送）
```
1. 添加商品到购物车
2. 去结算
3. 选择"商家外送"
4. 选择收货地址（如果没有则先添加）
5. 填写备注（可选）
6. 提交订单（状态：待支付，金额+3元配送费）
7. 选择支付方式并完成支付
```

### 流程3：充值余额
```
1. 个人中心 -> 点击余额卡片上的"充值"按钮
   或
   个人中心 -> 我的地址 -> 充值
2. 选择充值金额或输入自定义金额
3. 选择支付方式（微信/支付宝）
4. 确认充值
5. 余额更新
```

---

## 🔌 API接口说明

### 订单接口

| 方法 | 路径 | 说明 | 参数 |
|------|------|------|------|
| POST | `/order/submit` | 提交订单 | deliveryType, addressBookId, remark |
| GET | `/order/userPage` | 订单列表 | page, pageSize, status |
| GET | `/order/{id}` | 订单详情 | id |

### 支付接口

| 方法 | 路径 | 说明 | 参数 |
|------|------|------|------|
| POST | `/payment/balance` | 余额支付 | orderId |
| POST | `/payment/mock` | 模拟支付 | orderId, payType |
| POST | `/payment/recharge` | 余额充值 | amount, payType |
| GET | `/payment/balance` | 查询余额 | - |

### 地址接口

| 方法 | 路径 | 说明 | 参数 |
|------|------|------|------|
| POST | `/addressBook` | 新增地址 | consignee, phone, detail 等 |
| PUT | `/addressBook` | 更新地址 | id, consignee, phone 等 |
| DELETE | `/addressBook/{id}` | 删除地址 | id |
| PUT | `/addressBook/default` | 设为默认 | id |
| GET | `/addressBook/list` | 地址列表 | - |

---

## 📊 订单状态说明

| 状态码 | 状态名称 | 说明 | 颜色 |
|--------|---------|------|------|
| 1 | 待支付 | 订单已提交，等待支付 | 橙色 |
| 2 | 待派送 | 已支付，等待商家准备 | 蓝色 |
| 3 | 已派送 | 商家已派送/可取餐 | 绿色 |
| 4 | 已完成 | 订单完成 | 灰色 |
| 5 | 已取消 | 订单取消 | 红色 |

---

## 🛠️ 数据库更新

### 必须执行的SQL（如果还没执行）

```sql
-- 为orders表添加配送相关字段
USE reggie;

-- 添加配送方式字段
ALTER TABLE `orders` 
ADD COLUMN `delivery_type` int(11) DEFAULT 1 COMMENT '配送方式 1:到店自取 2:商家外送' AFTER `consignee`;

-- 添加配送费字段
ALTER TABLE `orders` 
ADD COLUMN `delivery_fee` decimal(10,2) DEFAULT 0.00 COMMENT '配送费' AFTER `delivery_type`;

-- 添加食堂ID字段
ALTER TABLE `orders` 
ADD COLUMN `canteen_id` bigint(20) DEFAULT NULL COMMENT '食堂ID' AFTER `delivery_fee`;

-- 添加食堂名称字段
ALTER TABLE `orders` 
ADD COLUMN `canteen_name` varchar(100) DEFAULT NULL COMMENT '食堂名称' AFTER `canteen_id`;

-- 修改address_book_id字段为可空（因为自取不需要地址）
ALTER TABLE `orders` 
MODIFY COLUMN `address_book_id` bigint(20) DEFAULT NULL COMMENT '地址id';
```

---

## 📝 文件清单

### 后端新增/修改文件

```
backend/src/main/java/com/java_project/reggie/
├── controller/
│   ├── PaymentController.java          (新增) 支付控制器
│   ├── AddressController.java          (修改) 添加更新、删除接口
│   └── OrderController.java            (修改) 返回订单详情
├── dto/
│   └── OrderDto.java                   (新增) 订单DTO
├── filter/
│   ├── JwtAuthenticationFilter.java    (修改) JWT认证
│   └── LoginFilter.java                (修改) 放行支付和地址接口
└── service/impl/
    └── OrderServiceImpl.java           (修改) 订单状态、金额计算
```

### 前端新增/修改文件

```
frontend/user/
├── pages/
│   ├── payment/                        (新增) 支付页面
│   │   ├── payment.wxml
│   │   ├── payment.wxss
│   │   ├── payment.js
│   │   └── payment.json
│   ├── recharge/                       (新增) 充值页面
│   │   ├── recharge.wxml
│   │   ├── recharge.wxss
│   │   ├── recharge.js
│   │   └── recharge.json
│   ├── address/                        (新增) 地址管理
│   │   ├── list.wxml/wxss/js/json
│   │   └── edit.wxml/wxss/js/json
│   ├── order/
│   │   ├── checkout.js                 (修改) 配送方式选择
│   │   ├── order.js                    (修改) 订单列表展示
│   │   ├── order.wxml                  (修改) 订单卡片布局
│   │   └── order.wxss                  (修改) 样式优化
│   └── user/
│       ├── user.wxml                   (修改) 显示余额和充值按钮
│       ├── user.js                     (修改) 加载余额数据
│       └── user.wxss                   (修改) 充值按钮样式
├── utils/
│   └── config.js                       (修改) 订单状态映射
└── app.json                            (修改) 注册新页面
```

---

## 🚀 启动步骤

### 1. 执行数据库更新
在MySQL中执行 `backend/添加配送字段.sql`

### 2. 启动后端
```bash
cd backend
mvn clean package
java -jar target/reggie-1.0-SNAPSHOT.jar
```

### 3. 启动前端
在微信开发者工具中打开 `frontend/user` 目录

---

## ✨ 功能特点

### 1. 完整的支付流程
- ✅ 多种支付方式
- ✅ 余额不足提示
- ✅ 支付成功反馈
- ✅ 自动更新订单状态

### 2. 用户体验优化
- ✅ 美观的UI设计
- ✅ 清晰的操作流程
- ✅ 友好的错误提示
- ✅ 实时余额更新

### 3. 安全性保障
- ✅ JWT认证
- ✅ 订单权限验证
- ✅ 地址权限验证
- ✅ 余额扣款事务处理

---

## 🐛 故障排除

### 问题1：支付时提示NOTLOGIN
**原因**：LoginFilter未放行支付接口  
**解决**：已在LoginFilter白名单中添加 `/payment/**`

### 问题2：订单列表商品名显示不正确
**原因**：后端未返回订单详情  
**解决**：后端返回OrderDto，包含订单明细列表

### 问题3：订单卡片超出边界
**原因**：CSS布局问题  
**解决**：优化order.wxss样式，使用flex布局

### 问题4：余额不显示或显示undefined
**原因**：用户信息接口未返回余额字段  
**解决**：确保User实体的balance字段不为null

---

## 📱 测试清单

### 基础功能测试
- [x] 用户登录
- [x] 添加商品到购物车
- [x] 提交订单（自取）
- [x] 提交订单（外送）
- [x] 添加收货地址
- [x] 设置默认地址
- [x] 编辑地址
- [x] 删除地址

### 支付功能测试
- [x] 余额支付（余额充足）
- [x] 余额支付（余额不足）
- [x] 微信支付（模拟）
- [x] 支付宝支付（模拟）
- [x] 支付成功后状态更新

### 充值功能测试
- [x] 快捷金额充值
- [x] 自定义金额充值
- [x] 充值后余额更新
- [x] 个人中心余额显示

### 订单列表测试
- [x] 订单列表展示
- [x] 按状态筛选
- [x] 显示真实菜品名
- [x] 待支付订单显示"去支付"按钮
- [x] 已完成订单显示"再来一单"按钮

---

## 🎉 总结

本次更新完成了：
1. **完整的支付系统**（余额+第三方模拟）
2. **余额充值功能**
3. **收货地址管理**
4. **订单列表优化**（显示真实数据、布局优化）
5. **订单状态流程**（从待支付到已完成）
6. **JWT认证修复**（所有接口正常工作）

系统现在已经具备完整的下单、支付、配送流程！🎊

