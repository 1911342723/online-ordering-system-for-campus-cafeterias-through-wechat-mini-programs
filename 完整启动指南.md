# 智慧食堂系统 - 完整启动指南

## 系统架构

```
智慧食堂系统
├── 后端 (Spring Boot + MyBatis-Plus)
├── 管理前端 (Vue + Vite)
└── 小程序前端 (微信小程序)
```

## 环境要求

- **Java**: JDK 8 或以上
- **MySQL**: 5.7 或以上
- **Node.js**: 14.0 或以上
- **Maven**: 3.6 或以上
- **微信开发者工具**: 最新版本

## 快速启动（3步完成）

### 第1步：数据库准备

```bash
# 1. 创建数据库
mysql -u root -p
CREATE DATABASE reggie CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
exit

# 2. 导入初始数据
mysql -u root -p reggie < backend/db.sql

# 3. 执行升级SQL（添加餐厅、余额等功能）
mysql -u root -p reggie < backend/db_update.sql
```

**验证**: 登录MySQL，查看表是否创建成功
```sql
USE reggie;
SHOW TABLES;  -- 应该看到 canteen、user、dish 等表
SELECT * FROM canteen;  -- 应该有3个餐厅
```

### 第2步：启动后端

#### 方式1: 使用启动脚本（推荐）

```bash
# Windows
启动后端.bat

# Linux/Mac
chmod +x 启动后端.sh
./启动后端.sh
```

#### 方式2: 使用IDEA

1. 打开 `backend` 项目
2. 配置 `application.yml` 数据库连接信息
3. 找到 `ReggieApplication` 主类
4. 右键运行

**验证**: 浏览器访问 http://localhost:8080，看到页面即成功

**端口**: 8080

### 第3步：启动管理前端

```bash
# 进入管理前端目录
cd frontend/admin

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

**验证**: 浏览器访问 http://localhost:3000

**默认账号**:
- 用户名: `admin`
- 密码: `123456`

**端口**: 3000

### 第4步：启动小程序

1. 打开微信开发者工具
2. 导入项目 → 选择 `frontend/user` 目录
3. AppID选择 "测试号" 或使用自己的AppID
4. 点击"编译"

**配置API地址**: 
打开 `frontend/user/utils/config.js`，确认baseURL：
```javascript
const BASE_URL = 'http://localhost:8080'
```

## 详细启动步骤

### 一、后端配置

#### 1. 配置数据库连接

编辑 `backend/src/main/resources/application.yml`:

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/reggie?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 你的MySQL密码
```

#### 2. 配置Redis（可选）

```yaml
spring:
  redis:
    host: localhost
    port: 6379
    password: 你的Redis密码（如果有）
    database: 0
```

#### 3. 配置豆包AI（可选）

编辑 `backend/src/main/resources/application.yml`:

```yaml
doubao:
  api-key: 你的豆包API Key
  api-url: https://ark.cn-beijing.volces.com/api/v3/chat/completions
  model: 你的模型ID
```

**注**: 如果不配置AI，系统会使用本地规则推荐作为降级方案

#### 4. 启动后端

```bash
cd backend
mvn clean package
java -jar target/reggie-1.0-SNAPSHOT.jar
```

或使用IDEA直接运行 `ReggieApplication.java`

**启动成功标志**:
```
Started ReggieApplication in X.XXX seconds
Tomcat started on port(s): 8080 (http)
```

### 二、管理前端配置

#### 1. 安装依赖

```bash
cd frontend/admin
npm install
```

**注意**: 如果安装速度慢，可以使用国内镜像：
```bash
npm install --registry=https://registry.npmmirror.com
```

#### 2. 配置API地址（已配置好）

`frontend/admin/vite.config.js`:

```javascript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, '')
    }
  }
}
```

#### 3. 启动开发服务器

```bash
npm run dev
```

**访问**: http://localhost:3000

### 三、小程序配置

#### 1. 配置后端API地址

编辑 `frontend/user/utils/config.js`:

```javascript
const BASE_URL = 'http://localhost:8080'  // 本地测试
// const BASE_URL = 'https://your-domain.com'  // 生产环境
```

#### 2. 导入小程序项目

1. 打开微信开发者工具
2. 点击"导入项目"
3. 项目目录选择: `frontend/user`
4. AppID: 选择"测试号"（或填入自己的AppID）
5. 项目名称: 智慧食堂

#### 3. 编译运行

点击工具栏的"编译"按钮，小程序即可在模拟器中运行

## 功能测试

### 1. 测试后端API

使用Postman或curl测试：

```bash
# 获取餐厅列表
curl http://localhost:8080/canteen/list

# 获取菜品分类
curl http://localhost:8080/category/list?type=1

# 获取菜品列表（第一食堂）
curl http://localhost:8080/dish/list?canteenId=1&status=1
```

### 2. 测试管理前端

1. 访问 http://localhost:3000
2. 登录（admin / 123456）
3. 测试功能：
   - 员工管理
   - 分类管理
   - 菜品管理
   - 订单管理

### 3. 测试小程序

#### 测试账号登录

手机号: `13800138000` （或其他测试账号）

#### 测试流程

1. **首页**
   - [ ] 查看餐厅列表（应该显示3个餐厅）
   - [ ] 点击餐厅进入菜单页

2. **菜单页**
   - [ ] 查看菜品分类
   - [ ] 查看菜品列表
   - [ ] 添加菜品到购物车
   - [ ] 查看购物车

3. **AI推荐**
   - [ ] 点击首页AI图标
   - [ ] 发送消息"推荐一些辣的菜"
   - [ ] 查看AI回复和推荐菜品

4. **用户中心**
   - [ ] 查看余额、优惠券、积分
   - [ ] 测试退出登录

5. **订单功能**
   - [ ] 提交订单
   - [ ] 查看订单列表
   - [ ] 查看订单详情

## 常见问题排查

### 后端启动失败

#### 问题1: 数据库连接失败
```
Error: Access denied for user 'root'@'localhost'
```

**解决**:
- 检查 `application.yml` 中的数据库密码
- 确认MySQL服务已启动
- 确认数据库 `reggie` 已创建

#### 问题2: 端口占用
```
Error: Port 8080 already in use
```

**解决**:
```bash
# Windows
netstat -ano | findstr :8080
taskkill /PID <进程ID> /F

# Linux/Mac
lsof -i :8080
kill -9 <进程ID>
```

#### 问题3: Redis连接失败
```
Error: Unable to connect to Redis
```

**解决**:
- 启动Redis服务
- 或者在 `application.yml` 中注释Redis配置

### 管理前端启动失败

#### 问题1: 依赖安装失败
```
Error: npm install failed
```

**解决**:
```bash
# 清除缓存重试
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

#### 问题2: 端口占用
```
Error: Port 3000 already in use
```

**解决**: 修改 `vite.config.js` 中的端口号

### 小程序问题

#### 问题1: 请求失败
```
Error: request:fail
```

**解决**:
1. 检查 `config.js` 中的 BASE_URL
2. 确认后端已启动
3. 在微信开发者工具中勾选 "不校验合法域名"

#### 问题2: 登录失败

**解决**:
1. 检查后端 `/user/login` 接口
2. 查看控制台错误信息
3. 确认测试用户已导入数据库

## 开发建议

### 后端开发
- 使用IDEA进行开发
- 开启热部署：在IDEA中配置 Spring Boot DevTools
- 查看日志：`backend/logs/` 目录

### 前端开发
- 使用VSCode进行开发
- 安装推荐插件：Vue Language Features (Volar)
- 查看网络请求：浏览器开发者工具 → Network

### 小程序开发
- 使用微信开发者工具
- 开启调试模式：详情 → 本地设置 → 不校验合法域名
- 查看日志：控制台 → Console

## 生产部署

### 后端部署
```bash
# 打包
mvn clean package -DskipTests

# 上传到服务器
scp target/reggie-1.0-SNAPSHOT.jar user@server:/path/to/app/

# 启动
nohup java -jar reggie-1.0-SNAPSHOT.jar > app.log 2>&1 &
```

### 前端部署
```bash
# 打包
npm run build

# 上传到Nginx
scp -r dist/* user@server:/usr/share/nginx/html/
```

### 小程序发布
1. 点击"上传"
2. 填写版本号和描述
3. 登录微信公众平台
4. 提交审核

## 技术支持

- 项目文档：查看各个目录下的 README.md
- API文档：`frontend/admin/API使用文档.md`
- 联调指南：`联调指南.md`

## 总结

启动顺序：
1. ✅ 导入数据库（db.sql + db_update.sql）
2. ✅ 启动后端（端口8080）
3. ✅ 启动管理前端（端口3000）
4. ✅ 启动小程序（微信开发者工具）

完成以上步骤后，系统即可正常运行！

